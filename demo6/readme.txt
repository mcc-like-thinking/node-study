## 主要功能介绍：
1、node连接数据库，修改数据库表的字段值
2、对html字符串格式化
3、读取本地文件内容，将读取内容更新至数据库

## 运行说明
1、安装相关依赖包： npm install
2、运行脚本（脚本切不可随意运行，导致数据库值被修改）
  # 运行index.js脚本： 命令行中输入 node index.js ，enter键确定运行
  # 运行localFileReplace.js脚本： 命令行中输入 node localFileReplace.js ，enter键确定运行
  